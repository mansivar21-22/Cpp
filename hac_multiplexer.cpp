#include<iostream>
#include<cmath>
using namespace std;
int main()
{
    int base , exp , result,num ;
    //cin >> base >> exp;
  //  pow(base, exp);
   // result = pow(base,exp);
    cin >> result;
    num = sqrt(result); 
    cin >> num;
    if (result == pow(base,exp))
    
        cout << exp;/* code */
    else
    
        cout << "Not possible";
    return 0;
}